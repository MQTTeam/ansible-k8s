apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: default
spec:
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32005
  type: NodePort   # 외부 노출 불필요, Nginx → Backend 내부 통신
